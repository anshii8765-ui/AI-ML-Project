# OCR
Place model weights in ocr/weights/ (don't commit large weights).

Example run:
  pip install -r requirements.txt
  python inference.py --input ocr/tests/sample.jpg

If model is stored on Drive/S3, add download command here.
